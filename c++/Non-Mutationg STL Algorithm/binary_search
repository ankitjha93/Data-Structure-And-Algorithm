#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int> v = {10,20,30,40,50};
    int x = 20;
    if(binary_search(v.begin(), v.end(), x) == true)
      cout<<"Found";
      else
       cout<<"Not Found";
    
  
       
    return 0;
}